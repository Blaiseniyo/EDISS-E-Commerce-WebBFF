type Book = {
  ISBN: string;
  title: string;
  author: string;
  description: string;
  genre: string;
  price: number;
  quantity: number;
  createdAt: Date;
  updatedAt: Date;
};

export default Book;
